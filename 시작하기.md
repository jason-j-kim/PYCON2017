# 시작하기 — 정책 아이디어 평가 시스템

이 폴더 하나에 **코드와 데이터가 모두 들어 있다.** 추가로 받을 파일은 없다.

---

## 1. 준비 (최초 1회)

### 필요한 것

| 항목 | 확인 |
|---|---|
| Python 3.10 이상 | `python --version` |
| Node.js 18 이상 | `node --version` |
| **Claude 구독 계정** | Pro 또는 Max — 본인 계정으로 로그인한다 |

### 설치

```bat
cd <이 폴더>
pip install -r webapp\requirements.txt
npm install -g @anthropic-ai/claude-code
claude
```

마지막 `claude` 실행 후 **`/login`** 을 입력해 본인 계정으로 로그인한다.
한 번 하면 유지된다.

### 점검

```bat
python check_setup.py
```

무엇이 준비됐고 무엇이 빠졌는지, 빠진 것은 어떻게 채우는지 알려준다.
**필수 조건 모두 충족**이 나오면 다음으로 넘어간다.

---

## 2. 실행

```bat
set SOCRATIC_ACCESS_CODE=원하는코드
python webapp\app.py
```

브라우저에서 **http://localhost:8000/policy**

끝낼 때는 그 창에서 **`Ctrl+C`**. 창을 X로 닫으면 포트가 남아 다음 실행에서
`10048` 오류가 난다. 그때는 `taskkill /F /IM python.exe`.

---

## 3. 외부에 공개하기

### 임시 주소 (준비물 없음)

새 명령창에서:

```bat
cloudflared tunnel --url http://localhost:8000
```

출력된 주소 뒤에 `/policy`를 붙여 쓴다. **켤 때마다 주소가 바뀐다.**

### 고정 주소 (Cloudflare 계정 + 도메인 필요)

```bat
setup_tunnel.bat
```

최초 1회만 실행하면 `run_tunnel.bat`이 만들어진다. 그 뒤로는 창 두 개만 띄우면 된다.

```bat
python webapp\app.py     REM 창 1
run_tunnel.bat           REM 창 2
```

주소가 `https://policy.본인도메인.com/policy` 로 **영구 고정**된다.

> **도메인이 없다면** 고정 주소는 두 가지 길뿐이다.
> ① 도메인을 하나 사서 Cloudflare에 등록한다(연 1~2만 원 수준).
> ② `worker/` 의 Workers 배포를 쓴다 — `*.workers.dev` 주소가 무료로 고정되지만
> **KDI 정밀 판정과 Word·PDF 불러오기가 빠진다.**

---

## 4. 들어 있는 데이터

네 통로 모두 **바로 작동한다.** 복사하거나 내려받을 것이 없다.

| 통로 | 데이터 | 위치 |
|---|---|---|
| ① 재정 | 세출예산 14,122개 사업 | `data/fiscal.json` |
| ② 정책연구 | KDI 발간물 7,362건 | `web/api/kdi.sqlite` |
| ③ 국회 의안 | 외부 API | 키 필요(선택) |
| ④ 해외사례 | OECD OPSI 1,015건 · 98개국 | `web/api/opsi_policies.db` |

③ 국회 의안만 키가 필요하다. 공공데이터포털에서 발급받아
`set ASSEMBLY_KEY=발급받은키` 로 지정한다. **없어도 나머지 세 통로는 정상 작동**하며,
꺼진 통로는 결과 화면에 "미실행"으로 표시되고 판정기가 확신도를 낮춘다.

> KDI 코퍼스는 본문(raw)을 덜어낸 경량본이지만, 판정에 쓰이는
> 제목·키워드·목차·요약이 그대로 있어 **kdinov 2차원 판정(중첩도 N0~N4 ×
> 역할)이 온전히 작동한다.** 원본 53MB는 필요 없다.

---

## 5. 공개 배포판 (선택)

Vercel에 올리면 상시 접속 가능한 공개판이 된다. `web/` 폴더를 Vercel 프로젝트로
연결하면 코퍼스가 함께 배포된다. 이용자는 **각자 자기 Claude API 키**를 브라우저에
입력한다(서버에 저장되지 않는다).

Cloudflare Workers 배포는 `worker/README.md` 참조 — 제약이 있으니 반드시 읽을 것.

---

## 6. 먼저 읽을 문서

| 순서 | 문서 | 내용 |
|---|---|---|
| 1 | `docs/연구자_인수_셋업가이드.md` | 인수·구동 상세, 자주 나는 오류 |
| 2 | `docs/HANDOFF_인수인계.md` | 시스템 현황, 실증 결과, **이미 내려진 결정 9건** |
| 3 | `docs/대화기반_아이디어평가_방법론.md` | 규칙 체계와 **미해결 쟁점 30항목** |

`HANDOFF` 6장의 결정들은 근거와 함께 정리돼 있으니 재검토하지 않아도 된다.

---

## 7. 손대지 말 것

- **체크리스트 30항목의 문안** — 바꾸면 기존 세션과 비교가 불가능해진다
- **가중치 35/35/30** — 세션 간 비교 가능성을 위해 고정
- 이미 배제된 선택지(봇 우회 수집, 외국 유료 정책DB) — 근거는 `HANDOFF` 6장

---

## 8. 연구 자료는 어디에 쌓이나

```
webapp/sessions.db        문답·채점·선례 판정 이력
```

터널판만 이력을 서버에 축적한다(Vercel·Workers는 브라우저에만 남는다).
읽기 좋은 형태로 보려면 `python webapp\show_session.py`.
